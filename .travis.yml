language: python
python:
  - "2.7"

install:
  - pip install -r requirements.txt
  - pip install pytest-cov pytest-pep8
  - pip install python-coveralls

script:
  - env PYTHONPATH=. py.test --cov corow --pep8 corow

after_success:
  - coveralls
